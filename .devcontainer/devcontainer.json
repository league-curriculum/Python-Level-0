{
    "name": "Python 3",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".." // Adjust this depending on the location of your Dockerfile relative to your project
    },
    "features": {
        "ghcr.io/devcontainers/features/desktop-lite:1": {}
    },
    "postCreateCommand": "cd docs && npm install",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python"
            ]
        }
    }
}
